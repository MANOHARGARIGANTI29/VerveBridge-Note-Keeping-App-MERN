// Exporting a constant named BASE_URL
export const BASE_URL = 'https://vervebridge-note-keeping-app-back-end.onrender.com/';
// This constant represents the base URL of the backend API for the note-keeping application.
// It is used to specify the root URL for making API requests from the frontend to the backend server.
// It should be updated if the backend server URL changes or if the application is deployed to a different environment.
